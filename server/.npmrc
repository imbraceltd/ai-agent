# Allow installation of platform-specific optional dependencies
target_platform=darwin
target_arch=arm64
optional=true

# Install optional dependencies (needed for DuckDB native bindings)
install-optional=true

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true
